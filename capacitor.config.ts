import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.grangemobile.app',
  appName: 'GrangeMobileIonic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
